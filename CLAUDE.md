# Range Soprano — Poker Range Study App

App de estudio de rangos preflop inspirada en FreeBetRange. Tres módulos: Visualizador, Entrenador, Editor. GTO y MDA están FUERA de alcance.

## Stack

React 19 + TypeScript estricto (noUncheckedIndexedAccess, exactOptionalPropertyTypes) · Vite · Tailwind 3 · Zustand 5 · React Router 7 · Zod · Lucide React · Inter (@fontsource).

## Estado de fases

1. ✅ Setup y fundamentos
2. ✅ RangeGrid completo (2A base, 2B stats, 2C a11y)
3. ✅ Zustand stores + persistencia
4. ✅ Editor (4A base · 4B pesos mixtos + undo/redo · 4C import/export)
5. ✅ Visualizador mejorado (5A scope · 5B filtros · 5C compare · 5D export PNG + atajos)
6. ✅ Entrenador (6A shell · 6B clásico · 6C dibujo · 6D filtros)
7. ✅ Pulido final (7A toasts · 7B a11y modals/menus · 7C error boundary + 404 + meta · 7D deploy GitHub Pages)
8. ✅ Editor avanzado (8A Ctrl+RightClick hand+ · 8B notas · 8C Save/Discard/Clear · 8D DnD carpetas/rangos · 8E acciones custom por rango)
9. ✅ Home + onboarding (módulos/atajos/portabilidad · import perfil JSON · donación BTC)
10. ✅ Villain + tableFormat HU/6max (NewRangeForm · PokerTable HU mode · filtro Viewer operativo)
11. ✅ Palette reuse · folder rename · meta edit-in-place · home docs
12. ✅ vs Limp · refactor SITUATION_LABELS · badges HU/villain en RangeManager · home tips
13. ✅ Trainer Speed mode (Classic 30s/60s/5min/10min · Drawing 30s/45s/60s/90s) + leaderboard local · Drawing session scoring + Ctrl+RightClick hand+ + paleta action-aware fix
14. ✅ Home FAQ + Overview performance/style (variant compact, React.memo) · Print PDF (logo accent, dimensiones por perPage, texto negro, chrome constreñido) · demo set 4 rangos en folder + sub-folders
15. ✅ SEO técnico (robots, sitemap, og-image, per-route titles via `useDocumentTitle` hook, FAQPage JSON-LD, pre-render rutas SPA en deploy.yml para HTTP 200, contenido educativo en home) · Import GTOWizard formato combo-por-combo (`AcKs: 0.78,...`) con agregación a hand-level, dispatch interno en `parseHandRange` sin tocar `ImportModal` · ImportModal multi-acción: N panes dinámicos (uno por `range.actions`) con dropdown + textarea + parsing independiente, un solo Apply con `pushHistory()` único y toast de combos clampeados cuando la suma >100% por celda · Fix export/import perfil JSON preserva `groupMeta` (colores/orden/colapso de carpeta) vía nuevo `zExportPayload` opcional y `mergeGroupMeta()` en uiStore — JSONs viejos sin el campo siguen siendo válidos
16. ✅ Pulido UX trainer (scroll-reset al cambiar rango via `key={range.id}` redundante en TrainerPage; ActionGrid rectangular `grid-cols-3` con botones `flex-row` swatch+label+pill en Classic + Speed Classic; card compactada `gap-4 p-4 sm:p-5`) · Branding en export PNG del Viewer (header con Spade morado + "Range Soprano" wordmark + "Poker Ranges" microcopy + nombre/subtítulo del rango + badge HU; render condicional vía `isExporting` + 2 RAFs antes de `toPng`, UI en pantalla sin cambios)
16a. ✅ Pot Odds Study trainer (4ª pestaña `Odds` en `/trainer` con icono Percent, range-independent — funciona sin rangos cargados · 4 tipos de pregunta: `bluff-fe` / `call-eq` / `bluff-size` / `call-size` con filter chips · MC de 4 opciones con distractores por proximidad de bucket en `SIZINGS` · display values hardcoded en `BLUFF_FE_DISPLAY` y `CALL_EQ_DISPLAY` matcheando la tabla canónica para evitar drift de floating-point · FeedbackPanel con fórmula sustituida `bet/(pot+bet)=0.5/(1+0.5)=33%` · atajos 1-4 / Enter / N · score in-memory sin persistir, leaderboard pendiente para 16b)
16b. ✅ Pot Odds Speed mode + leaderboard (sub-pestaña Speed dentro de Odds via `OddsTrainer` wrapper con sub-toggle Study/Speed · 3 fases config/running/finished espejo de SpeedTrainer · duraciones 30s/60s/120s · timer `setInterval(100ms)` con `performance.now()` y `doneRef`/`scoreRef` · MistakesPanel con prompt + picked vs correct + fórmula reproducible al final · leaderboard separado en `range-soprano/odds-leaderboard` indexado por `durationSec` (no por rangeId) · top 5 por duración, sort accuracy → correct → qpm · `EMPTY_ENTRIES` con `Object.freeze` para estabilidad de selector Zustand)
16c. ✅ Pulido Pot Odds Study (MiniPot visual con dos barras horizontales Pot/Bet escaladas dinámicamente — overpot 1.5x/2x se pinta amber — solo en preguntas directas vía campo nuevo `visualSize?: Sizing` en OddsQuestion · Auto-avance toggle 1.5s en Study cancelable manual via Next/Enter/N · Streak bonus visual cuando `score.streak >= 5`: card "Streak" se enciende amber con Trophy icon · helper `sizingFraction()` exportado desde potOdds)
16d. ✅ Freestyle input numérico en Pot Odds Study (toggle "Modo experto" al lado de Auto-avance que reemplaza los 4 chips MC por `<input type="number">` con autoFocus · tolerancia ±1% via `parsePct()` sobre `question.correct` · solo aplica a kinds directas `bluff-fe` / `call-eq` — las inversas se filtran del pool de generación cuando expert ON · effect que redibuja inmediato si activás expert con sizing question en pantalla · auto-OFF cuando el usuario apaga ambas directas en KindFilter · FeedbackPanel extendido con prop opcional `picked` para mostrar "Tipeaste X%" en errores · Speed mode no recibe freestyle)
16e. ✅ Export/Import JSON del leaderboard de Pot Odds Speed (botones Download/Upload en header del componente `Leaderboard`, visibles en config y finished · Export descarga `odds-leaderboard-YYYY-MM-DD.json` con `{ version: 1, exportedAt, byDuration }` · Import valida con `zOddsLeaderboardExportPayload`, dedupea por `dateIso`, ignora duraciones no soportadas, mergea respetando cap top 5 por duración — no reemplaza · método nuevo `mergeImport()` en `oddsLeaderboardStore` retorna conteo de inserciones para feedback de toast · reusa `downloadBlob` / `todayIsoDate` y patrón de file picker de `ImportProfileButton`)
17. ✅ Traducción completa UI a español latinoamericano voseo (Sidebar nav Inicio/Visualizador/Entrenador/Editor; mode tabs Trainer Clásico/Dibujo/Velocidad/Pot Odds y sub-tabs Estudio/Velocidad en Odds; stats Precisión/Correctas/Racha/Mejor racha/Acierto/Falso positivo-negativo/Manos/min/Manos totales/Pintadas/Tamaño del rango; Viewer tabs Individual/Comparar/Resumen y botones Exportar PNG/Imprimir PDF; Editor descripción + hint largo + meta-form Propiedades/Posición/Situación/Villano + "— Ninguno —" + "No aplica a RFI" + "Etiquetas de impresión"/"Stack (izq)" + NewRangeForm Nombre/Paleta inicial/Cancelar/Crear; SituationSelector filtros + "Cualquiera" + Limpiar; Home module cards Visualizador/Entrenador/Editor + descripciones traducidas; toasts/confirms/aria-labels; `formatRelativeDate` "ahora"/"hace Xm-h-d"; términos preservados en inglés: Stack, Sizing, Call/Raise/Fold, RFI, HU, posiciones BTN/CO/HJ/etc · `lang="es"` ya estaba — sin impacto en SEO, URLs sin cambios)
17b. ✅ Traducciones pendientes home + menús editor (menú ··· de rango: Renombrar/Mover a grupo…/Duplicar/Eliminar; menú ··· de carpeta: Renombrar carpeta…; confirm/toast de delete/rename en español; aria-labels y placeholders del form inline de RangeManager; HomePage: nombres de módulos Visualizador/Entrenador en FAQ + shortcuts + howto; tabs Viewer Individual/Comparar/Resumen y modos Trainer Velocidad/Dibujo en sección how-to; "tabla de líderes local" en howto; labels UI "Mover a grupo…"/"Nombre"/"Renombrar carpeta…" en sección Organizar rangos; fix frase "jugás" → "que decidís jugar" en ¿Qué es un rango?)
17c. ✅ QR de donación BTC en home (single source of truth) — generación client-side via `qrcode.react` (`QRCodeSVG`, ~7KB gzip, MIT, cero deps transitivas, cero network calls) desde la misma const `BTC_ADDRESS` que alimenta `<code>` visible + `copyToClipboard` · URI BIP21 `bitcoin:${BTC_ADDRESS}` para auto-fill en wallets · tile 140px `bg-white p-3 ring-1 ring-border` con caption "Escaneá con tu billetera" · layout `flex-col sm:flex-row` (mobile vertical, desktop QR a la izquierda + address+copy a la derecha con `flex-1`) · level "M" 15% error correction · seguridad: imposible divergir QR vs address sin tocar la const (1 línea de diff visible en code review), sin imagen externa hijackeable, address visible debajo del QR como verificación canónica)
18a. ✅ Push/Fold scaffold (Nash HU, 5ª pestaña `/trainer` con icono Crosshair, range-independent — funciona sin rangos cargados como Pot Odds · datos canónicos en `src/data/nashTable.ts` formato row-major 13×13 espejo de la imagen del usuario, 169 cells × 2 charts (PUSH/CALL) convertidos a `Record<HandNotation, number>` via `gridCoordsToHand` · convención: threshold > 0 = "push/call cuando stack ≤ threshold", `0` = "nunca push/call en el rango", `20` = saturación (siempre push/call dentro del rango estudiado) · helpers `nashPushBB` / `nashCallBB`, `shouldPush` / `shouldCall`, `pickStackBB` con sesgo ±2.5 al threshold 70% del tiempo + uniforme 30% para mantener anchors obvios · store `pushFoldLeaderboardStore` espejo del de Odds, key `range-soprano/pushfold-leaderboard`, top 5 por `durationSec`, `EMPTY_ENTRIES` frozen para selector estable, `mergeImport` con dedupe por `dateIso` · constantes `PUSHFOLD_DURATIONS [30,60,120]`, `PUSHFOLD_KINDS ['push-or-fold','call-or-fold']` · footnotes `*63s`/`*53s`/`*43s` (exclusion bands del chart fuente) documentadas pero no modeladas — se usa solo el upper bound)
18b. ✅ Push/Fold Estudio: Tabla + Quiz (chip toggle interno Tabla|Quiz dentro del wrapper `PushFoldStudy` · Tabla con slider stack 1-20 BB step=1 + scope toggle Push BTN verde / Call BB azul con iconos `ArrowUpFromLine` / `ArrowDownToLine` + toggle "Mostrar BB" que agrega threshold debajo del label en celdas activas con color `text-amber-200` font-bold drop-shadow para contraste sobre fondos verde y azul saturados · grid 13×13 propio en `PushFoldTable.tsx` que NO reusa `RangeGrid` — control directo de paint-by-threshold via `data-active` y category bg para celdas inactivas, evita la complejidad de `actionsMap` y permite overlay del threshold sin absolute positioning · stats arriba: % combos en rango / manos en rango / combos absolutos · Quiz binario en `PushFoldQuiz.tsx` con KindFilter push-or-fold | call-or-fold (mín 1 activo) · prompt con eyebrow contextual `BTN · push or fold · 12.0 BB` + frase + `HandBadge` con la mano · 2 botones binarios Push/Call (verde o azul según scope) vs Fold (gris) con atajos `1`/`2` + Enter/Space/N · FeedbackPanel cita threshold via `describeThreshold()` · ScoreBar Precisión/Correctas/Racha/Mejor con streak bonus amber a partir de 5)
18c. ✅ Push/Fold Velocidad + leaderboard persistente (3 phases config/running/finished espejo del patrón OddsSpeed · duraciones 30/60/120s · timer `setInterval(100ms)` con `performance.now()` + `doneRef` para evitar double-finish + `scoreRef`/`onFinishRef`/`enabledRef` para mantener efectos sin re-suscribir a props mutables · botones binarios con flash 220ms y auto-advance al next · header mono grande con timer + progress bar + danger color cuando ≤5s · MistakesPanel collapsable reproduce cada error con mano + stack BB efectivo + picked→correct + frase del threshold tipo "AKo: push hasta 20 BB · te tocó 14.5 BB" · Leaderboard top 5 por duración con highlight de la última corrida + botón Clear duración · sin export/import del JSON — diferido a posible 18d)
18d. ✅ Push/Fold cartas visuales + stack chip + auto-avance (HandBadge textual reemplazado por dos cartas reales con palos visibles vía `CardFace` + `parseHandCards` extraídos de `PokerTable.tsx` a nuevo módulo `src/modules/trainer/HandCards.tsx` · convención de palos del Clásico: ♠♥ pares, ♣♣ suited, ♥♠ offsuit · stack BB gana chip dedicado al lado de las cartas con label STACK arriba y "X.X BB" en `font-mono text-2xl font-bold tabular-nums text-amber-200` — mismo lenguaje BB-amber del threshold de Tabla y streak bonus · sentence sin BB redundante, eyebrow sin BB · Auto-avance toggle (1.5s) en PushFoldQuiz + CountdownBar morada animada bajo el feedback, mismo patrón de ClassicTrainer · `CountdownBar` extraído a `src/modules/trainer/CountdownBar.tsx` reusado en ClassicTrainer/OddsStudy/PushFoldQuiz)
18e. ✅ Pot Odds prompt rediseñado + auto-avance default ON (`OddsQuestion` gana campos estructurados `scenarioLabel`/`scenarioValue`/`question.{lead,keyword,tail}` sin romper `prompt` legacy usado para a11y y `MistakesPanel` · render extraído a `src/modules/trainer/OddsPrompt.tsx` compartido entre Estudio y Velocidad · ScenarioChip con label muted arriba + valor amber-200 bold 2xl abajo · pregunta corta con keyword en `text-accent-light font-semibold` · MiniPot rediseñado como mesa real online: pot stack 100 BB anclado al centro + bet stack proporcional al sizing con fichas apiladas (1 ficha/20 BB, cap 10), `tone='amber'` cuando overpot 1.5x/2x · grid 3-col con disambiguation espacial — villano bet a la izquierda, hero bet a la derecha · auto-avance arranca activado por default en Pot Odds Estudio y Push/Fold Estudio)
19. ✅ Fix SEO indexación GSC (sitemap listaba `/viewer` `/trainer` `/editor` sin trailing slash pero GitHub Pages emite 301 a las versiones con barra → "Error de redirección" en GSC para `/viewer` y duplicate indexing en `/editor`+`/trainer` · `<link rel=canonical>` estático en `index.html` apuntaba a `/` para todas las rutas SPA, reforzando la confusión · Fix combinado: trailing slash en `public/sitemap.xml` para las 3 SPA routes + `useDocumentTitle` extendido con opción `canonical?: string` que setea el `<link>` en runtime y restaura al default `https://rangesoprano.com/` en cleanup · ViewerPage/TrainerPage/EditorPage pasan canonical canónico self-referente · `index.html` y pre-render loop de `deploy.yml` no se tocan)
20. ✅ Randomizador en /viewer Resumen — tarjeta fija ~420px en el slot `actions` del PageHeader cuando `viewMode === 'overview'` con 3 filas: 4 presets pill editables (default `60/40 · 50/50 · 25/75 · 10/90`) con auto-label inteligente (cambiar valor regenera label `${v}/${100-v}` salvo custom) · botón Tirar + valor mono amber-200 + Auto (0.5/1/2/5s) · Set tabs 1/2/3 persistidos + toggle Eye on/off del highlight verde de presets matched + gear ⚙ con popover absolute (no empuja layout) para frecuencia, editor del set y restaurar · atajos `Espacio`/`A`/`Esc` · store nuevo `randomizerStore` con persist + Zod (key `range-soprano/randomizer`) y schema sin `.strict()` para tolerar fields legacy · feature "Vincular rango" + lógica `evaluateAgainstCell` descartadas tras validación con uso real (5 clicks por mano vs lectura visual ~1s del OverviewTile + roll + preset matched) · Lista de N rolls también descartada (sin uso claro contra 4 presets simultáneos) · portabilidad: `zExportPayload` extendido con `randomizer.optional()` + action `applyImportedConfig` en el store — el `.json` de Editor → Export → Download all ranges incluye sets/frecuencia/highlight y se restaura al importar el perfil completo · home actualizada con sección dedicada "Randomizador — mixed strategies en mesa" + FAQ "¿Cómo funciona el randomizador?"
21. ✅ Cronómetro de sesión en /viewer Resumen (card ~260px en slot `actions` del PageHeader junto al RandomizerPanel · play/pause/flag/reset con horas/min/seg/centisegundos · vueltas solo cuentan tiempo en running para excluir descansos automáticamente · panel de vueltas absoluto sin desplazar layout · persiste en `range-soprano/chronometer` con offset correcto al recargar mientras corre · sin atajos de teclado para evitar conflicto con Espacio del randomizador)
22. ✅ Pop out combinado cronómetro + randomizador via Document Picture-in-Picture (botón `PictureInPicture2` redondo absolute en esquina superior-derecha del header de /viewer Resumen · ventana flotante siempre-encima en Chrome 116+/Edge/Brave/Opera, fallback `window.open()` en Firefox/Safari · clona stylesheets del head padre + replica `data-theme` del `<html>` para CSS vars · React Portal mantiene subtree en mismo árbol → Zustand singletons compartidos sin código custom de sync · pill "En ventana flotante · Volver" en el header mientras está abierta · cleanup al desmontar `FloatingTools`)
23. ✅ UX de carpetas en /editor (rename de carpeta con doble click sobre el botón del título → panelito con `<select>` Padre con opción "— Sin padre (raíz) —" filtrado anti-ciclo + input Nombre + línea Resultado en vivo · sin menú `…` colgante huidizo · `renameGroup`/`renameGroupMeta` del store ya manejan reanidado prefix-aware sin tocar · validación anti-ciclo doble: filtrado del dropdown + chequeo en `commitFolderRename` con toast error · toast contextual `"movida dentro de X"` / `"movida al nivel raíz"` / `"renombrada a X"` · tree compacto: `paddingLeft` step `16 → 12 px` en FolderRow + wrapper de hijos en `renderFolder` simplificado eliminando `marginLeft: (depth+1)*16+4` + `paddingLeft: 8` + `border-l border-border` que se sumaban al indent del FolderRow hijo causando ~104px de offset a depth 3 con nombres rotos verticalmente · `renderRangeRow(s, indentLevel=0)` aplica `paddingLeft: indentLevel * 12` al `<li>` para alinear rangos hijos con sub-folders hermanos sin pisar el drag handle · tree guide lines verticales 1px `bg-border/50` ancladas al centro del chevron del folder padre — `left: node.depth * 12 + 32` donde `32 = 22 drag handle invisible + 4 button px-1 + 6 chevron center` — `top:0 bottom:1` dejando 4px de respiro al siguiente hermano · home actualiza item "Renombrar o mover carpeta" al flujo nuevo + barra de progreso de donaciones integrada en la columna derecha de "Apoya el proyecto" debajo de Copiar — single source of truth en `src/data/donations.ts` con 4 constantes editables, framing "Meta 2026" para vida útil anual, USD principal + BTC en pie con link "verificar" a blockchain.com)
24. ✅ Sección Calculadoras (5ª ruta `/calculadoras` con icono `Calculator` en sidebar, range-independent · matemática pura en `src/utils/ev.ts` con `evBasic` / `evComplex` / `impliedOdds` / `floatEv` / `combinedFoldEquity` · **5 sub-tabs** internas con `ModeToggle` espejo de `OddsTrainer` — EV básico (`$W·W% − $L·L%`), EV con fold equity (`F%·Pot + C%·($W·W% − $L·L%)`), Implied Odds (`call/eq − call − pot` con warning si supera el stack del villano), EV de flotar (`Barrel%·(−call) + X/F%·pot − Resto%·(turnBet+call)` con `Resto%` derivado mostrado en `<ReadOnlyField>`, espejo del Excel del usuario que verifica $41 con D7=55/D8=50/D9=75), Fold equity combinada multi-way (`∏ fold_i` sobre jugadores con dato, 6 campos opcionales — los vacíos se ignoran, no se multiplican por 0) · cada calc usa `useState<string>` para permitir edición intermedia tipo `1.` sin clamping, parseo a número vía `parseField()` con min/max · campos auto L%/C%/Resto% en `<ReadOnlyField>` no editables · resultado en `<ResultCard>` con tone positive/negative/neutral + `<FormulaDetails>` colapsable con fórmula canónica + valores sustituidos + resultado · sin persistencia: cada cálculo es de un solo uso, los inputs se desmontan al cambiar de sub-tab · SEO: pre-render de `/calculadoras` en `deploy.yml` + entry en `sitemap.xml` con trailing slash priority 0.7 + `useDocumentTitle` con canonical self-referente · HomePage: 4ª `ModuleCard`, grid breakpoint `sm:grid-cols-2 xl:grid-cols-4`, FAQ nueva "¿Cómo uso las calculadoras de EV?" cubriendo las 5 herramientas · ronda de español Latinoamérica + voseo sobre strings que se escaparon — incluidos eyebrows pre-existentes `Math · pot odds` / `Math · ICM short stack` en TrainerPage y `en el sidebar` en instrucciones de carpeta de HomePage)
25. ✅ All-in EV calc (6ª sub-tab de `/calculadoras` con icono `Bomb`, entre EV con fold equity e Implied Odds · cierra el item 11 diferido de fase 24 · `allInEv({pot, call, shove, equityPct, foldPct})` en `src/utils/ev.ts` con fórmula del Excel `=(C7*C3)+((1-C7)*C6*(C5+C3-C4))-((1-C7)*(1-C6)*C5)` → `EV = F·Pot + (1−F)·Eq·(Pot+Shove−Call) − (1−F)·(1−Eq)·Shove` · **breakeven F% analítico** sin Solver: despeje `F* = B/(B−A)` con `A=Pot, B=Eq·(Pot+Shove−Call)−(1−Eq)·Shove`, devuelve raw value y la UI maneja 4 casos contextuales (negativo → "+EV en cualquier fold equity", >100% → "imposible incluso con 100% fold", `null` → sin solución finita, dentro de [0,100] → caption con el valor) · `SensitivityTable` local con 13 escenarios del Excel (−10B / −10F / −10E / −5B / −5F / −5E / Actual / +5E / +5F / +5B / +10E / +10F / +10B), clamp `[0,100]` en equity y folds, columna `Actual` con `ring-1 ring-accent`, tonal `emerald-300` / `rose-300` / `content-muted` por signo · sin chart de barras — `<table>` con `overflow-x-auto` cubre la lectura sin recharts · `useState<string>` + early-return en `useMemo` para narrowing TS estricto · sanity defaults del Excel: EV=$22.57, Breakeven=56.1% (el 66% del Excel era Solver viejo, analítico es siempre fresco) · HomePage `ModuleCard` "Cinco" → "Seis herramientas" + FAQ JSX + `aPlain` sincronizados — convención `feedback_calculators.md`)
26. ✅ Tres calcs nuevas en `/calculadoras` (6 → 9 sub-tabs) migrando los Excels restantes del usuario · **Multi-way Call EV** (icono `GitFork`, label "Call multi-way") `multiWayCallEv({pot, call, huEquityPct, othersCallPct, mwPot, mwEquityPct})` → `EV = pot·huEq·(1−oc) − call·(1−huEq)·(1−oc) + (mwPot−call)·mwEq·oc − call·(1−mwEq)·oc` espejo del Excel `=(C3*C6*(1-C8))-(C4*(1-C6)*(1-C8))+((C9-C4)*C10*C8)-(C4*(1-C10)*C8)` modelando el split HU vs MW con dos equities distintas; en lugar de SensitivityTable lleva 3 `ScenarioCards` (HU only · Actual con `ring-accent` · MW only) overrideando `othersCallPct` a 0/actual/100, replicando el chart de barras del Excel sin recharts; sanity defaults $11.00 ✓ ($9 HU/$11 actual/$14 MW) · **EV de bluff** (icono `VenetianMask`, entre EV con fold equity y Check vs Bet) `bluffEv({pot, bet, foldPct})` → `EV = F·Pot − (1−F)·Bet` (caso degenerado de `evComplex` sin equity en showdown — river bluffs / blocker bets) devuelve `{ev, pickupAmount, lossAmount, breakevenFoldPct}` con breakeven analítico `F* = bet/(bet+pot)` siempre finito en [0,100] si pot+bet>0; UI con ReadOnly C% y bet/pot% + `Breakdown` panel espejo del "Because..." del Excel (+emerald pickup / −rose loss) + breakeven F% en caption del ResultCard; sanity defaults pot=100/bet=75/fold=45% → +$45 / −$41.25 / EV=$3.75 / BE=42.9% ✓ · **Check vs Bet** (icono `Scale`, calc dual con pot compartido) dos funciones puras `checkRiverEv({pot, winPct})` → `pot·win%` y `betRiverEv({pot, bet, winWhenCalledPct, foldPct})` → `F·Pot + (1−F)·W·(Pot+Bet) − (1−F)·(1−W)·Bet` reframeando `evComplex` para río (win% cuando te pagan = 0% bluff puro, alto valuebet); UI con dos paneles lado a lado bajo un único input pot, dos ResultCards en `sm:grid-cols-2` + `RecommendationCard` con tone accent que resuelve `check`/`bet`/`tie` (`Math.abs < 0.005`) mostrando el delta de EV; sanity Excel pot=100/checkWin=25%/bet=50/wc=0%/fold=20% → EV check $25 / EV bet −$20 / recomendación "Checkear gana por $45.00" ✓ · HomePage `ModuleCard` "Seis" → "Nueve herramientas" + FAQ JSX + `aPlain` sincronizados (convención `feedback_calculators.md`) · sin tocar sitemap/deploy/canonical — todas son sub-tabs internas de `/calculadoras`)

27. ✅ Call vs Raise EV (10ª sub-tab de `/calculadoras` con icono `Swords`, entre All-in EV e Implied Odds · 2ª calc dual del proyecto tras Check vs Bet · migra los dos bloques restantes del Excel de río del usuario: "THE EV OF CALLING THEIR RIVER BET" + "THE EV OF SHOVING OVER THEIR RIVER BET" · lado **Call** = nueva `callRiverBetEv({pot, call, equityPct})` → `Pot·eq − Call·(1−eq)` espejo del Excel `=(D7*D5)-(D6*(1-D7))`; lado **Raise** reutiliza `allInEv` tal cual (fórmula exacta del shove + breakeven F% gratis) sin tocarla · **Pot** y **Monto a pagar** compartidos arriba en `grid sm:grid-cols-2` (mismo villain bet, no duplicados); dos paneles Call (Equity al pagar + ReadOnly Lose%) / Raise (Tu all-in total + Win% si te pagan + F% fold + ReadOnly continúa%) · dos `ResultCard` por signo, la de Raise lleva `caption` con breakeven F% (4-case display abreviado del de AllInEvCalc) · `RecommendationCard` mejora el Excel: resuelve **el mejor de {Call, Raise, Fold=0}** vía `sort` por ev — si ambos EV son negativos recomienda **Fold**; empate con `|delta| < 0.005` · sanity Excel defaults pot=150/call=50/eqCall=25%/shove=200/winWhenCalled=25%/fold=50% → EV Call $0 ✓ / EV Raise $37.5 ✓ ($38 redondeado) / recomendación Raise por $37.5 · términos Call/Raise/Fold en inglés (glosario fase 17) · HomePage `ModuleCard` "Nueve" → "Diez herramientas" + FAQ JSX + `aPlain` sincronizados (convención `feedback_calculators.md`) · sin tocar sitemap/deploy/canonical — sub-tab interna de `/calculadoras`)

28. ✅ Raise sizing & pot odds (11ª sub-tab de `/calculadoras` con icono `ChevronsUp`, entre Call vs Raise e Implied Odds · combina las **3 calcs "vs Raise flop"** del Excel del usuario en una sola tab con inputs compartidos Bote + Bet + Raise size · tres funciones puras nuevas en `ev.ts`: `raisePctOfPot({bote,bet,raiseSize})` → `raise/(3·bet+bote)·100` (Excel `=E67/((3*E66)+E65)`), `raiseSizeFromPct({bote,bet,pctOfPot})` → `(3·bet+bote)·%` (inverso, Excel `=(3*E74+E73)*E76`), `potOddsVsRaise({bote,bet,raiseSize})` → `(raise−bet)/(2·raise+bote)·100` (Excel `=(E82-E81)/(2*E82+E80)`) · denominadores `3·bet+bote` (sizing) y `2·raise+bote` (pot odds) replicados literal del Excel, expuestos en `FormulaDetails` sustituido para transparencia · guard `denom>0` evita NaN/Infinity · UI: inputs compartidos en `grid sm:grid-cols-3`, dos ResultCards (% del pot del raise · equity para pagar vs raise) tone neutral con caption explicativa, sección "Conversor inverso" con input % objetivo + ResultCard de fichas (helper `formatChips()` local — round 1 decimal + " fichas"), sección Fórmulas con 3 `FormulaDetails` · sanity Excel defaults bote=10/bet=3/raise=9 → 47% del pot ✓ / 21% equity ✓; %objetivo=47 → 8.9 fichas ✓ · HomePage `ModuleCard` "Diez" → "Once herramientas" + FAQ JSX + `aPlain` sincronizados (convención `feedback_calculators.md`) · sin tocar sitemap/deploy/canonical — sub-tab interna de `/calculadoras`)

29. ✅ Doble barrel turn + river (12ª sub-tab de `/calculadoras` con icono `Flame`, justo después de EV de bluff · migra los dos bloques del Excel "EV del bet Turn" + "EV de Bet turn + Barrel River" · `doubleBarrelEv({potTurn, betTurn, foldTurnPct, betRiver, foldRiverPct})` en `ev.ts` devuelve `{evTurnOnly, evCombined, riverPot, pmeTurnPct, pmeRiverPct}` · **evTurnOnly** = `fold_t·Pot_t − (1−fold_t)·Bet_t` (bluff puro de una calle, Excel `=E93*E90-(1-E93)*E91` = −12.5) · **evCombined** = `fold_t·Pot_t + (1−fold_t)·fold_r·(Pot_t+Bet_t) − (1−fold_t)·(1−fold_r)·(Bet_t+Bet_r)` (Excel descompuesto `=F90+H99-H100` = 6.25, verificado con contabilidad de fichas calle por calle: línea fold-turn gana Pot_t, línea call-turn/fold-river gana Pot_t+Bet_t = riverPot−Bet_t, línea call-call pierde Bet_t+Bet_r en showdown con 0 equity) · **riverPot derivado** = `Pot_t + 2·Bet_t` mostrado en `<ReadOnlyField>` (no editable, evita inconsistencia) · **PME** = `bet/(pot+bet)` por calle = fold breakeven del bet como bluff puro, en ReadOnlyField (turn 33.33% / river 42.86%) · UI: dos paneles Turn/River en `sm:grid-cols-2`, dos ResultCards (EV turn solo · EV combinado) tone por signo, `InsightCard` con 4 mensajes contextuales según signos (el barrel rescata / ambas +EV / ni el barrel rescata / mejor checkear river) · objetivo del usuario: ver que la línea completa puede ser +EV aunque el bet del turn solo sea −EV · sanity Excel defaults potTurn=100/betTurn=50/foldTurn=25%/betRiver=150/foldRiver=50% → EV turn −12.5 ✓ / EV combinado +6.25 ✓ / riverPot 200 ✓ · HomePage `ModuleCard` "Once" → "Doce herramientas" + FAQ JSX + `aPlain` sincronizados (convención `feedback_calculators.md`) · sin tocar sitemap/deploy/canonical — sub-tab interna de `/calculadoras`)

30. ✅ Value / Bluff combos máx (13ª sub-tab de `/calculadoras` con icono `Ratio`, después de Doble barrel · migra el Excel "Calculadora de Value / Bluff" · objetivo del usuario: saber cuántos combos de farol meter respecto a los de valor sin pasarse ni quedarse corto · `valueBluffCombos({pot, bet, valueCombos})` en `ev.ts` devuelve `{maxBluffCombos, bluffFreqPct, callFreqPct, oddsRatio}` · **maxBluffCombos** = `value · bet/(pot+bet)` (Excel `=E116*E113/F116` simplificado algebraicamente: `bluffFreq/(1−bluffFreq)·value` con `bluffFreq=bet/(pot+2bet)` colapsa a `value·bet/(pot+bet)`) = 40·64/164 = 15.61 ✓ · **bluffFreqPct** = `bet/(pot+2·bet)` = 28.07% (% del rango de apuesta que son faroles, Excel `=E112/(E111+2*E112)`) · **callFreqPct** = `pot/(pot+bet)` = 61% (MDF del villano, Excel `=-((E112/(E112+E111))-1)`) · **oddsRatio** = `(pot+bet)/bet` = 2.56 (precio del call) · sanity cruzado: 15.61/(15.61+40) = 28.07% = bluffFreq ✓ · UI: inputs Bote/Bet/Value combos en `grid sm:grid-cols-3`, ResultCard headline de bluff combos máx, sección "Frecuencias óptimas" con 3 ReadOnlyFields (frecuencia bluff · MDF · odds), sección Fórmula · **extra sobre el Excel**: campo opcional "Bluff combos actuales" + `InsightCard` de 3 casos (balanceado emerald / te pasás rose con cuántos sacar / bluffeás de menos amber con cuántos agregar) — sirve directo al objetivo "no pasarme ni meter menos" · cada caso cuantifica el costo como cota superior: under-bluff suma **fold equity en juego** = `faltantes · bote` ("dejás hasta $X" si el villano sobre-foldea), over-bluff suma `excedente · bet` ("regalás hasta $X cuando te pagan" si el villano sobre-paga) + **nota GTO bidireccional** al pie aclarando que en equilibrio el farol es break-even (0 EV vs MDF), que bluffeás para no ser explotable, y los dos modos de explotación (de menos → sobre-foldeo / de más → sobre-pago) — los costos se realizan solo cuando el villano se desvía · `formatCombos()` local (2 decimales trim) · HomePage `ModuleCard` "Doce" → "Trece herramientas" + FAQ JSX + `aPlain` sincronizados (convención `feedback_calculators.md`) · sin tocar sitemap/deploy/canonical — sub-tab interna de `/calculadoras`)

31. ✅ Fold equity requerida con equity (14ª sub-tab de `/calculadoras` con icono `Shield`, label "FE requerida", después de All-in EV · objetivo: entender que el breakeven de fold equity **baja** cuando tenés equity al shovear all-in flop/turn · `foldEquityRequired({pot, shove, equityPct})` en `ev.ts` deriva del break-point `EV = −x + F·(x+1) + (1−F)·E·(1+2x) = 0` → `F = [x − E·(1+2x)] / [1 + x − E·(1+2x)]` con `x = shove/pot`, `E = equity` · con E=0 colapsa a `x/(1+x) = bet/(pot+bet)` (breakeven de bluff puro) · devuelve `{shoveFractionPct, breakevenFoldPct, breakevenFoldNoEquityPct, alwaysProfitable}` · breakevenFoldPct puede ser **negativo** (+EV con 0 folds) o **null** cuando `denom ≤ 0` (equity domina, +EV siempre) — la UI muestra "≤ 0%" + caption · UI: inputs Bote/Shove/Equity en `grid sm:grid-cols-3`, ResultCard headline "Break-even fold (con tu equity)" tone emerald si alwaysProfitable + ReadOnlyField al lado con la referencia sin equity (bluff puro), `InsightCard` que verbaliza la baja "sin equity necesitarías F0%, con tu E% baja a F% — Δ menos gracias a tus outs" · validación cruzada: matchea el Excel (E=35/shove=50/pot=100 → −25%, E=0 → 33.3%) **y** el "fold equity calculator" web de referencia (E=10/shove=150/pot=100 → 52.4% ✓) · HomePage `ModuleCard` "Trece" → "Catorce herramientas" + FAQ JSX + `aPlain` sincronizados (convención `feedback_calculators.md`) · sin tocar sitemap/deploy/canonical — sub-tab interna de `/calculadoras`)

32. ✅ Pulido de claridad en calcs existentes (A+B+F, sin sub-tabs nuevas, tras auditar las capturas de Excel del usuario) · **A — captions de interpretación**: helper `evInterpretation(ev)` en `CalcShared` ("a largo plazo ganás/perdés $X en promedio cada vez (+EV/−EV)" o break-even) aplicado en `ResultCard` de EV básico / EV con fold equity / EV de flotar — las tres mostraban el $ con color pero sin guía de lectura, ahora consistentes con las nuevas · **EV básico reframeado a poker concreto** (pedido del usuario: "decisión binaria/embolsás/tirás" sonaba abstracto) → labels Pot / % que esperás ganar / Tu apuesta, defaults pot=100·40%·50, ReadOnlys "% que esperás perder" + "Breakeven equity" + "Tu apuesta como % del pot" para lectura visual · **B — raise% en Check vs Bet**: `betRiverEv` extendido con `raisePct?` opcional → `EV = F·Pot + C·(W·(Pot+Bet)−(1−W)·Bet) − R·Bet` con `C=1−F−R` (R=0 colapsa al fold/call previo, backward-compat); UI suma campo "R% — Frecuencia de raise" (foldeás perdiendo tu bet) + ReadOnly "C% te paga" = 100−F−R con warning si F+R>100 + guard `betResult=null` · matchea el Excel "Bet vs Check IP" exacto (pot275/bet100/fold42/raise10/W100 → 285.5 ✓, verificado con node) · **F — PME/breakeven consistente**: ReadOnly "PME — pot odds que ofrecés" = `bet/(pot+bet)` en Check vs Bet (lado bet), ReadOnly "PME — equity para pagar" = `call/(pot+call)` en Call vs Raise (lado call), ReadOnly "Breakeven W% — equity para EV 0" = `$L/($W+$L)` en EV básico · **decisión pendiente confirmada**: las calcs NUEVAS (C/D/E: EV de checkear compuesto, EV del raise con combos, EV conjunto multi-street) van a separar inputs "Bote / Apuesta del villano" como el Excel — diferido a la próxima fase · sin tocar sitemap/deploy/canonical, sin sub-tabs nuevas)

33. ✅ Tres calcs nuevas (C/D/E, 14 → 17 sub-tabs) migrando los bloques restantes del Excel "EV del call/check/bet/raise + conjunto" del usuario · todas **separan Bote / Apuesta del villano** como inputs distintos (pedido explícito del usuario, a diferencia de las viejas que piden el pot combinado) · **C — EV de checkear** (icono `Hand`, después de Check vs Bet) `checkCompoundEv({pot, villainBetsPct, villainBet, callEquityPct, checkCheckEquityPct})` → `pBet·[eqCall·(pot+vbet)−(1−eqCall)·vbet] + (1−pBet)·[eqXx·pot]` — junta las dos ramas de checkear: check-call si el villano apuesta, check-check si checkea atrás; dos ResultCards de rama + total con `evInterpretation`; sanity Excel 55.24 ✓ · **D — EV del raise** (icono `ArrowBigUp`, después de Raise sizing) `raiseBluffEv({pot, villainBet, raiseCost, foldPct})` → `F·(pot+vbet) − (1−F)·raiseCost` (bluff-raise, 0 equity si pagan) + `breakevenFoldPct = raiseCost/(raiseCost+pot+vbet)`; toggle "derivar fold% de combos" via `foldPctFromCombos(bet,call)=1−call/bet`; sanity Excel −2.20 / breakeven 52.94% / combos fold 42% → −0.75 ✓ · **E — EV multi-calle** (icono `Workflow`, después de Doble barrel) `multiStreetEv({evTurn, seeRiverPct, evRiver}) = evTurn + (seeRiver/100)·evRiver` — encadena dos calles; campos EV permiten negativos (parseField sin min); ReadOnly "aporte del river"; sanity Excel 5.18+0.6·(−81.4)=−43.66 ✓ · todas verificadas con node antes de commitear · **EV básico reframeado** (mismo commit, pedido del usuario): descripción sin "decisión binaria", labels Pot/% que esperás ganar/Tu apuesta · HomePage `ModuleCard` "Catorce" → "Diecisiete herramientas" + FAQ JSX (3 spans nuevos + EV básico desc actualizada) + `aPlain` resincronizado (estaba stale en "doce") · sin tocar sitemap/deploy/canonical · **pulido UX post-feedback**: "checkea atrás" → "check behind" (jerga); home hero `text-justify` via nuevo prop `descriptionClassName` en `PageHeader`; EV del raise defaults a pot=100; EV multi-calle defaults coherentes con Doble barrel (−12.5 / 75% / 25 → +6.25, reproduce el combinado) · **selector reordenado a orden pedagógico data-driven**: `CALC_GROUPS` array con 5 grupos (Fundamentos · Cuando apostás vos · Cuando enfrentás una apuesta · All-in · Multi-way) agrupados por la acción que tenés enfrente, con etiquetas de grupo (span `w-full` que fuerza salto de línea en el flex-wrap, `text-[10px]` uppercase muted) — editar el array reordena el selector; CalcMode union y conditionals de render reordenados para coincidir; pills inactivas con `ring-inset ring-border/50` para delimitar)

34. ✅ Módulo "Análisis de manos" — fase 1: Worksheet desde `.txt` (6ª ruta `/analisis` con icono `FileSearch` en sidebar, range-independent) · objetivo: convertir una mano real jugada en un estudio estructurado apoyado en las 17 calcs, SIN reconstruir herramientas que el usuario ya tiene (replayer de PokerTracker, Flopzilla) — la equity la sigue trayendo de Flopzilla y la tipea · **34a** `src/utils/handHistory.ts` parser tolerante del formato iPoker (cartas palo-primero `[HJ S9]`, suits H/S/C/**D** → ♥♠♣♦, rango `10`→`T`; motor de pot por contribución corrida — `potBefore` por decisión es lo que siembra las calcs; `Decision`/`StreetData`/`ParsedHand`; héroe del `Dealt to`, board acumulado, showdown) · `src/utils/spotCalc.ts` `suggestCalcForDecision(decision, hand)` mapea cada jugada del héroe a la calc adecuada con **seeds enriquecidos** (cada seed lleva pot/bet/currentPot/loseAmount + potTurn/betTurn/betRiver para que TODA alternativa reciba datos, no solo la primaria — fix tras feedback) + `barrelPairSeed` reconstruye la línea de 2 barriles en ambas direcciones + `flopzillaInputsFor(mode)` lista qué inputs trae el usuario de Flopzilla por calc; reglas: bet→bluff-ev (o double-barrel si hay barrel en calle siguiente), allin→all-in-ev, call→call-vs-raise, **check en river→check-vs-bet/check-ev** (check behind vs valuebet fino) · `CalcMode`+`CALC_GROUPS`+`CALC_META` extraídos a `src/modules/calculators/calcMeta.ts` compartido · verificado con node: hero J♥9♠, board A♥7♥9♣3♥8♠, pots 70/140/350 · **34b** `CardFace` extendido a ♦ (aditivo, deck 4 colores ♠slate/♥rojo/♦azul/♣verde, no afecta al trainer que solo usa 3) · `src/modules/analysis/` BoardCards (reusa CardFace) · InlineCalc (switch CalcMode→componente sembrado) · HandWorksheet (cabecera con cartas+stacks+toggle Fichas↔BB, calles con board+badge de pot ámbar, decisiones con monto en chip ámbar + sizing `% pot`, botón Analizar inline con chips de calc + `DataSourceLegend` verde/ámbar que indica qué está cargado de la mano y qué ingresar de Flopzilla, notas por mano, showdown) · AnalysisPage (textarea + "Cargar ejemplo" + parse tolerante con banner suave de errores) · 8 calcs ganan props `initial*` opcionales (`?: string | undefined` por exactOptionalPropertyTypes) backward-compatible — `/calculadoras` queda idéntico, render con `key={decision.id}-${mode}` para re-sembrar al cambiar · ruta + nav · **34c** SEO: `sitemap.xml` `/analisis/` priority 0.7 + pre-render en `deploy.yml` + canonical self-referente en AnalysisPage · HomePage `ModuleCard` "Análisis de manos" + FAQ "¿Cómo analizo una mano que jugué?" (JSX + `aPlain` sincronizados) · **34d** (`47e6b16`) parser generalizado a **GGPoker/PokerStars** (formato que exporta PT4) además de iPoker, sin romperlo: cartas order-agnostic (`[Js Td]` rango+palo-min vs `[HJ S9]` palo-may+rango — el palo es el char en {s,h,c,d}), board acumulado (parsea todos los grupos `[...]` y reemplaza/concatena según `BOARD_SIZE` de la calle — GG repite todo el board, iPoker solo la nueva), header `Poker Hand #` + ciegas de `Level1(10/20)`, botón desde `Seat #N is the button`, all-in flag + `Uncalled bet (X) returned to` (resta del pot), shows inline del runout + summary GG `Seat N: name (pos) showed [..] won (X)` + `collected X from pot` · worksheet: call preflop que enfrenta raise (call>BB) ahora analizable + board final en Showdown · **34e** (`a73daaa`) tercer dialecto **Winamax**: acciones SIN dos puntos (`Atenea. posts small blind 40` — se prueba `Nombre: verbo` y se acepta el colon solo si lo sigue un verbo conocido, porque las descripciones traen `" : "` tipo `Two pairs : 5 and 4`; fallback a match anclado al verbo), header `Winamax …` con ciegas en `(40/80)` (parse genérico de parénthesis-slash), botón en `Table:`, board pegado `[4d Js Td][7c]`, `collected X from main/side pot`, `won X` sin paréntesis, shows con desc en paréntesis · **34f** (`32ba57c`) cuarto dialecto **PokerStars** (cash + spins): header `PokerStars Hand #`, ciegas con currency opcional (`($0.02/$0.05 USD)` cash vs `(15/30)` torneo), montos con `$` en uncalled/collected/won (regexes con `[€$£]?`), **split pots** (`collected` del mismo jugador se suma main+side, `won` del summary es fallback), `posts small & big blinds`/`mucks`/`joins the table` manejados · **4 salas soportadas: iPoker + GGPoker/PokerStars(GG) + Winamax + PokerStars(nativo)** · próxima fase elegida por el usuario: Modo Workbook con drills, empezando por "¿Qué calculadora?" (reusa `spotCalc.ts`)

35. ✅ Módulo "Ejercicios" — fase 1 del Workbook: drill **"¿Qué calculadora?"** (7ª ruta `/ejercicios` con icono `Dumbbell` en sidebar, range-independent · **módulo nuevo y separado de `/analisis`** que queda intacto · objetivo del usuario: entrenar el reflejo de elegir la herramienta de EV correcta para cada spot postflop) · **sin leaderboard ni persistencia** (decisión del usuario) — score/racha en memoria · `src/modules/workbook/quizSpots.ts` banco de 5 manos en formato iPoker parseadas al cargar con `parseHandHistory` → cada decisión **postflop** del héroe pasa por `suggestCalcForDecision` (la MISMA lógica que el worksheet, así la respuesta correcta nunca diverge) · solo postflop: los opens/3bets preflop quedan fuera (no son spots de estas calcs) · `generateQuizQuestion` arma MC de 4: correcta = `suggestion.primary`, 3 distractores sesgados al mismo grupo de `CALC_GROUPS` **excluyendo `suggestion.alternatives`** (las alternativas válidas no aparecen como opción → los incorrectos son inequívocamente incorrectos) · helpers `describeSpot`/`formatAmount`/`boardForDecision` (BB cuando hay ciegas, si no fichas) · `src/modules/workbook/WhichCalcDrill.tsx` espejo de `OddsStudy`: spot card (mano + board + frase), 4 opciones con icono+label de `CALC_META` (atajos 1-4), `FeedbackPanel` con el `rationale` de spotCalc + `flopzillaInputsFor(correct)`, ScoreBar (precisión/correctas/racha/mejor con bonus ámbar ≥5), auto-avance 2s + `CountdownBar`, Enter/N para avanzar · `WorkbookPage.tsx` + `useDocumentTitle` canonical · **descarté botón "Abrir esta calc"** en el feedback (el link a `/calculadoras` no quedaría pre-llenado — eso es de `/analisis` — el rationale + inputs ya enseñan) · verificado con node: pool de 9 preguntas, cobertura de las 5 calcs (double-barrel/bluff-ev/all-in-ev/call-vs-raise/check-vs-bet), invariantes OK en 4000 generaciones · SEO: `sitemap.xml` `/ejercicios/` priority 0.7 + pre-render en `deploy.yml` + HomePage `ModuleCard` "Ejercicios" + FAQ "¿Cómo practico elegir la calculadora correcta?" (JSX + `aPlain` sincronizados) · **mismo commit, pulido de copy pedido por el usuario**: FAQ "¿Qué es Range Soprano?" actualizada (decía "Tres módulos", ahora refleja los 6 + matemática); hero principal suma Ejercicios; ronda de residuos de voseo que se escaparon de la migración (`exportá/Construí/importá/Configurá/entrá/dimensioná/generá/volvé/borralos→bórralos` y `acá→aquí` en viewer/editor/home/calc); `/analisis` eyebrow `workbook`→`Estudio de manos`, menciona `.txt` de **PokerTracker 4**, `del héroe→de hero`, `la tipeas→la ingresas`, label `Héroe→Hero`, **mano de ejemplo anonimizada** (`Balans3→hero`, `denf0rdz→villano` — verificado que parsea idéntico); `/ejercicios` eyebrow `Workbook→Entrenamiento`) · próxima fase: más drills del Workbook (conteo de combos, value/bluff, fold equity numérica) y/o soporte de más salas en el parser (888/partypoker)

36. ✅ Módulo "Ejercicios" — 2º drill: **"Conteo de combos"** (bloqueadores) dentro de `/ejercicios` vía `DrillToggle` (tabs "¿Qué calculadora?" | "Conteo de combos", espejo del `SubModeToggle` de `OddsTrainer`) · decisiones del usuario (AskUserQuestion): se cuenta una **mano nombrada** (AKs/77/AJo/`AK`=suited+offsuit), NO clases de mano hecha (set/color quedan fuera — serían motor tipo Flopzilla); bloqueadores = **board + tus cartas (hero)** · `src/utils/comboMath.ts` combinatoria pura con bloqueadores (reusa `isPair`/`isSuited` de handUtils, `Card`/`Suit`/`cardToString` de handHistory, `RANKS` de poker): `comboCount(hands, dead)` suma combos restantes — par `n·(n−1)/2` con `n`=palos disponibles del rank; suited = palos donde ambos ranks disponibles; offsuit `a·b−sameSuit` · `makeDeck`/`deadSetOf`/`shuffle`/`baseComboCount` · `src/modules/workbook/comboSpots.ts` `generateComboQuestion`: elige tipo de objetivo + ranks, reparte hero(2)+board(3/4/5) **sesgando 0–2 bloqueadores** de los ranks del objetivo (si no, siempre da la base), `dead=hero∪board`, `correct=comboCount`, `blockedBy`=cartas muertas de esos ranks; distractores = `baseCombos` (trampa "olvidaste bloqueadores") + vecinos `correct±1..3` clampeados a `[0,baseMax]`, 4 únicas mezcladas · `src/modules/workbook/ComboCountDrill.tsx` espejo de `WhichCalcDrill` (ScoreBar/AutoAdvance/CountdownBar/atajos 1-4·N): spot card "¿Cuántos combos de AK quedan?" + "Tus cartas"/"Board" con `BoardCards`, 4 opciones numéricas, `FeedbackPanel` descompone **Base N − bloqueados M (por cartas) = quedan** · verificado con node: casos canónicos (AK/A♥=12, 77/un 7=3, A5s/A♠+5♥=2, AJo/A♥+J♥=6) + invariantes en 20k generaciones (correcta∈opciones, 4 únicas, recomputación independiente coincide) · WorkbookPage description generalizada + HomePage `ModuleCard`/FAQ ampliadas a los 2 drills (FAQ renombrada "¿Qué ejercicios de práctica hay?", JSX+`aPlain` sync) · sin tocar ruta/sitemap/deploy/canonical (sub-drill interno) · próximos drills: value/bluff, fold equity numérica, textura/SPR

37. ✅ Módulo "Ejercicios" — 3er drill: **"Value / Bluff"** (balance del rango de apuesta en el river) · 3ª tab del `DrillToggle` (icono `Scale`) · reusa `valueBluffCombos` de `ev.ts` (single source of truth con `/calculadoras`): pregunta "con V combos de valor apostando X% del bote, ¿cuántos faroles para balancear?" → `faroles = valor·bet/(pot+bet)` · `src/modules/workbook/valueBluffSpots.ts` `generateValueBluffQuestion`: elige sizing (33/50/67/75%/pot/1.5x/2x) + value combos (6..24), llama `valueBluffCombos({pot:1,bet:frac,valueCombos})`, redondea a 1 decimal; **distractores = trampas conceptuales** (usar la frecuencia `bet/(pot+2bet)` como conteo, olvidar el bet en el denominador `bet/pot`, doble del correcto) + vecinos `±0.5`, 4 únicas · `ValueBluffDrill.tsx` espejo numérico de ComboCountDrill: spot card con chips Apuesta/Valor (sin cartas), `FeedbackPanel` con la fórmula sustituida + ratio valor:farol (medio bote≈3:1, pot≈2:1, overbet≈1.5:1) + % del rango que son faroles · **refactor de limpieza (mismo commit)**: extraído el shell compartido de los 3 drills — `drillScore.ts` (Score/INITIAL_SCORE/tallyScore/AUTO_ADVANCE_MS/STREAK_BONUS_THRESHOLD, sin JSX) + `drillUi.tsx` (ScoreBar/AutoAdvanceToggle, solo componentes para no romper react-refresh) — los 3 drills importan de ahí en vez de copiar ~80 LOC c/u · verificado con node: ratios canónicos (½ bote 3:1, pot 2:1, 2x 1.5:1) + invariantes en 20k generaciones (4 únicas, correcta presente, recomputación coincide) · HomePage `ModuleCard`/FAQ ampliadas a los 3 drills (JSX+`aPlain` sync) · **decisión del usuario: el soporte de más salas del parser de `.txt` YA NO es pendiente** (tiene las principales — iPoker/GG/Stars/Winamax — y agregará una nueva solo si la pide puntualmente; no recordárselo proactivamente) · próximos drills: fold equity numérica (`bluffEv`/`foldEquityRequired`), textura/SPR

38. ✅ Módulo "Ejercicios" — 4º drill: **"Fold equity"** (auto-profit / FE mínima de un bluff) · 4ª tab del `DrillToggle` (icono `Shield`; el toggle pasó a `flex-wrap` para no desbordar en móvil con 4 tabs) · reusa `bluffEv` de `ev.ts`: pregunta con **fichas concretas** (distinto del value/bluff abstracto) "apuestas {bet} en un bote de {pot} como bluff puro, ¿qué % de fold para break-even?" → `breakevenFoldPct = bet/(pot+bet)` (el alpha) · `src/modules/workbook/foldEquitySpots.ts` `generateFoldEquityQuestion`: pot de un set + sizing (33/50/67/75%/pot/1.5x/2x), `bet=round(pot·frac)`, `correct=round(bluffEv(...).breakevenFoldPct)`; **distractores = trampas** (la MDF complemento `100−correct` = frecuencia de defensa, y `bet/pot` por olvidar el bet en el denominador) + vecinos ±1, 4 únicas en [0,100] · `FoldEquityDrill.tsx` espejo numérico (opciones en %, chips Bote/Tu apuesta), `FeedbackPanel` con `bet/(pot+bet)` sustituido + atadura a la **MDF** (el complemento es lo que el villano debe defender) · verificado con node: BE folds canónicos (½ bote 33%, pot 50%, ⅓ 25%, 2x 67%) + invariantes en 20k (4 únicas, correcta presente, `correct+mdf=100`, recomputación coincide) · HomePage `ModuleCard`/FAQ a los 4 drills (JSX+`aPlain` sync) · próximos drills: textura de board / SPR

39. ✅ Módulo "Ejercicios" — 5º drill: **"SPR / compromiso"** (EV de comprometerse, port de la sección "SPR: EV Practice" del workbook) · 5ª tab del `DrillToggle` (icono `Gauge`) · **límite de alcance confirmado**: NO deriva equity vs rango (eso es Flopzilla) — la equity se **da como dato**, se entrena la matemática del compromiso + la decisión a cada SPR · reusa `allInEv` (shove) y `callRiverBetEv` (pagar all-in) de `ev.ts` — cero matemática nueva; verificado que reproduce el libro exacto ($151 call SPR 1.0 / $258 shove SPR 1.7) · **mezcla de 2 tipos de pregunta** (decisión comprometerse/fold con botones binarios · valor del EV en $ con MC de 4) × **2 situaciones** (shove con fold equity / pagar all-in) · `src/modules/workbook/sprSpots.ts` `generateSprQuestion`: pot+SPR→stack, equity 25-72%, fold 20-70% (shove); SPR=stack/pot mostrado como contexto; **decisión balanceada ~50/50 commit/fold** (apunta a un target y reintenta con margen `|EV|>10% pot` para evitar coin-flips — clave porque sin balancear el ~90% sería commit); ev-value distractores = trampas (shove: olvidar fold equity = `allInEv` con fold 0; call: solo la ganancia sin restar pérdida) + vecinos, negativos permitidos · `SprDrill.tsx` espejo del shell, render condicional binario/MC, `FeedbackPanel` con EV sustituido + zona de compromiso ("SPR ≤2 te comprometés con menos equity; fold=0") · verificado con node: números del libro + invariantes en 40k (recomputación coincide, balance 52/48, sin ambiguos) · HomePage `ModuleCard`/FAQ a los 5 drills · **textura de board: descartada por el usuario** (sería motor tipo Flopzilla) · drills del Workbook: **completos por ahora** (¿Qué calc? · Combos · Value/Bluff · Fold equity · SPR) · **39b** (pedido del usuario) botón "Ver el cálculo en la calculadora" en **3 drills** (estilo el "Analizar" de `/analisis`): tras responder, despliega inline la calc adecuada **pre-llenada con los números del spot** · componente genérico `CalcReveal` extraído a `drillUi.tsx` (toggle + panel colapsable, recibe la calc sembrada como `children` — solo se monta al abrir, reaplica seed; cada drill maneja su `showCalc` y la pausa de auto-avance) · mapeos fieles al libro/calc: **SPR** shove → `AllInEvCalc` (Complex EV) / pagar → `EvBasicCalc` (Basic EV) · **Fold equity** → `BluffEvCalc` (pot/bet → breakeven fold%) · **Value/Bluff** → `ValueBluffCalc` (sembrado con par pot/bet canónico agregado a cada `Sizing`, bet/pot === frac) · calcs extendidas con props `initial*` aditivas (`EvBasicCalc.initial{WinAmount,WinPct,LoseAmount}`, `AllInEvCalc.initial{EquityPct,FoldPct}`, `ValueBluffCalc.initial{Pot,Bet,ValueCombos}`; `BluffEvCalc` ya tenía pot/bet) — backward-compat, `/calculadoras` y `/analisis` intactos · aparece **después** de responder (no spoilea, decisión pedagógica: la respuesta es función determinística de los datos mostrados, así que pre-llenar antes = spoiler → es una "hoja de respuestas" post-recall) y **pausa el auto-avance** al abrirse · reproduce exacto el resultado del drill (Combos no tiene calc equivalente; ¿Qué calc? queda fuera por ahora) · **fix Fold equity**: la calc se sembraba con `F%=45` (default de `BluffEvCalc`) → mostraba un EV arbitrario; como la respuesta del drill ES el break-even fold (independiente del F% que ingreses, depende solo de pot/bet), ahora se siembra `initialFoldPct` con el break-even exacto `bet/(pot+bet)` a 2 decimales → la calc muestra **EV=$0**, coherente con la pregunta (verificado: redondea a $0 en todos los sizings)

40. ✅ Reorganización en **dos bloques** (Rangos / Matemáticas) + migración de Pot Odds y Push/Fold de `/entrenador` a `/ejercicios` · objetivo del usuario: la web se lee como dos ejes — **Rangos** (crear/ver/entrenar rangos: Visualizador·Entrenador·Editor) y **Matemáticas** (Calculadoras·Análisis·Ejercicios) · Pot Odds y Push/Fold eran range-independent y ya estaban framados como "Matemática · …" → pertenecen al bloque mate · **decisión: solo re-rutear navegación, NO mover archivos** — los componentes siguen en `src/modules/trainer/` (`OddsTrainer`, `pushfold/PushFoldTrainer`) y `/ejercicios` los importa (igual que ya hacía con `CountdownBar`); los leaderboard stores ya viven en `src/store/`, no se tocan · **TrainerPage**: `TrainerMode` baja a `'classic'|'drawing'|'speed'`, se borran los dos `early return` de odds/pushfold y el caso especial `ranges.length===0 && mode!=='odds'…` se simplifica a `ranges.length===0` — el Entrenador queda solo con los 3 modos basados en rangos · **WorkbookPage**: `Drill` se extiende con `'pot-odds'|'push-fold'`; el `DrillToggle` pasa de lista plana a **dos grupos rotulados** data-driven (`CONCEPT_DRILLS` = los 5 drills MC sin leaderboard · `TABLE_DRILLS` = Pot Odds + Push/Fold, entrenadores con sub-toggle Estudio/Velocidad + leaderboard) separados por un divisor; wrapper `max-w-3xl` para drills y `max-w-5xl` para las tablas (vía `WIDE_DRILLS` set) porque la grilla 13×13 de Push/Fold y los leaderboards necesitan aire · **Sidebar**: `NAV_ITEMS` plano → `NAV_SECTIONS` con encabezados de sección (Inicio suelto · Rangos · Matemáticas), rótulos ocultos en `collapsed` con divisor 1px de respaldo · **HomePage**: ModuleCard Entrenador sin Pot Odds, ModuleCard Ejercicios suma Pot Odds+Push/Fold; FAQ "¿Sirve para aprender pot odds?" y "¿Qué ejercicios hay?" reapuntadas a `/ejercicios` (grupo Tablas), howto "Pot odds" linkea a Ejercicios→Tablas→Pot Odds; `aPlain` JSON-LD sincronizado · **sin tocar rutas/sitemap/canonical/deploy** (Pot Odds y Push/Fold siempre fueron pestañas, no rutas → cero impacto SEO; URLs `/trainer/` y `/ejercicios/` sin cambios) · lint pre-existente de `react-hooks/refs` en `OddsSpeed`/`PushFoldSpeed` (patrón `scoreRef`/`doneRef`/`onFinishRef` intencional) NO introducido por esta fase

41. ✅ Módulo "Ejercicios" — 6º drill de Conceptos: **"Runouts"** (probabilidad de turn / river / completo) · port del Cap. 9 de *The Postflop Poker Workbook* (Runouts: Turn / River / Complete) · objetivo: internalizar **con qué frecuencia sale un runout** — próxima carta en turn (1/47), en river (1/46) y runout completo desde el flop (2 cartas, **método del complemento** `100−no·no`), contando bloqueadores + conexión con la regla 4/2 · **`src/utils/runoutMath.ts`** (motor puro, espejo de `comboMath.ts`): `RANK_INDEX`/`rankValue`, `unseenAfter`, **`makesStraight`** (evaluador de escalera wheel-aware — NO existía hand-eval en el repo, único componente nuevo no trivial ~20 LOC), predicados de una carta (`isSuit`/`pairsBoard`/`isOvercard`/`isUndercard`/`isAbove`/`isAtOrBelow`/`completesFlush`/`completesStraight`), `countSingle` + enumeración `tallyPairs` (C(47,2)=1081) con combinadores `eitherCard`/`bothCards`/`runnerFlush`/`runnerStraight` · **`src/modules/workbook/runoutSpots.ts`** generador (espejo de `comboSpots.ts`): `RunoutPhase = turn|river|complete`, single kinds suit/pair-board/overcard/undercard/flush/straight, complete kinds suit/pair/overcard/flush/runner-flush/both-low/zero-overcards · deal aleatorio para categorías, **construcción por plantilla** para proyectos (`buildFlushDraw` exactamente 4 del palo, `buildStraightDraw` ventana de 4 consecutivas + bricks con retry+verificación por el engine, `buildBackdoorFlush` 3 del palo, `buildPairLowBoard` par + board sin overcards) · `correct` siempre computado por el engine (nunca por la construcción) · **MC con trampas conceptuales** `makePctOptions`: turn-only % (olvidar el river), complemento 100−N, bloqueador olvidado, denominador /52 · `breakdown` discriminado (single/complement/pairs) para el feedback · **`RunoutsDrill.tsx`** espejo de `ComboCountDrill` (ScoreBar/AutoAdvance/CountdownBar/atajos 1-4·N, sin CalcReveal — no hay calc de probabilidad), spot card con mano + board + tiles **"X"** para la(s) carta(s) por venir, FeedbackPanel que muestra el conteo/denom (single), el complemento sustituido + atajo 4/2 (complement), o hits/total (pairs), **filtro multi-select de fases** (Turn/River/Completo, mín 1, patrón KindFilter de OddsStudy; no se redibuja al togglear — el próximo draw honra el set, evita setState-in-effect) · 6º tab del grupo Conceptos en `WorkbookPage` (icono `Shuffle`) · **verificado con node**: reproduce EXACTO 28 respuestas impresas del libro (overcard 21/30/35, undercard 43/38/26/24, color 19/23/17/21, emparejar 15/19/22/26, escalera 17/9, flush completo 35, ambas 2-6 18, cero overcards a QQ 69) + invariantes en 60k generaciones (4 opciones únicas, correcta presente, denom 47/46 correcto, pct en (0,100), cobertura de los 19 phase:kind) · HomePage ModuleCard + FAQ a 6 conceptos (JSX + `aPlain` sync) · sin tocar rutas/sitemap/canonical/deploy (sub-drill interno)

42. ✅ Módulo "Ejercicios" — 7º drill de Conceptos: **"Floating"** (EV de flotar) · port del Cap. 10 de *The Postflop Poker Workbook* (Floating) · objetivo: calcular la EV de flotar (pagar un cbet con aire y apostar la siguiente calle cuando te checkean) según las frecuencias barrel / check-fold del villano · modelo de **3 ramas** (suman 100%): check/fold → ganás bote−call; barrel → perdés call; check/continúa → perdés call+bet · **reúso puro de `floatEv` de `ev.ts`** (fase 24) — la fórmula YA reproduce el libro exacto; el "matiz" pot vs pot−call era solo mapeo de input: `potOnFlop` de la calc = pot **antes de tu call** (los "$155" del libro = potOnFlop 115 + call 40) · `src/modules/workbook/floatSpots.ts` generador (espejo de `sprSpots.ts`): `FloatStreet=turn|river`, `FloatKind=decision|ev-value`, board+nextCard cosméticos con `scenarioLabel` veraz (`describeNextCard` reusa `RANK_INDEX`), `potOnFlop`/`call`/`bet`/`barrel`/`xf` sanos, **roundInt half-away-from-zero** para matchear el libro (−9.5→−10) · decision balanceado ~50/50 con margen; ev-value MC con **trampas conceptuales** (olvidar la rama check-continúa, no restar el call en la rama de fold, vecinos) · `FloatDrill.tsx` espejo de `SprDrill` (mix decisión binaria Flotar/Foldear · MC de valor · ScoreBar/AutoAdvance/CountdownBar/atajos), spot card con board + **next card resaltado** (ring acento) + chips de frecuencia y montos, FeedbackPanel con las 3 ramas sustituidas + "suman 100%" + takeaway (EV alta con muchas cartas similares: overcards en flops bajos, bricks en AK6 — ata con Runouts) · **CalcReveal** → `FloatEvCalc` pre-llenada (le agregué props `initial*` aditivas) que reproduce el EV del spot · 7º tab del grupo Conceptos (icono `Sailboat`) · **verificado con node**: 12 respuestas impresas del libro reproducidas EXACTO + invariantes en 40k (ev-value 4 únicas con correcto, recomputación coincide, freqs suman 100, decision 50/50) · HomePage ModuleCard + FAQ a 7 conceptos (JSX + `aPlain` sync) · **pulido de claridad post-feedback** (convención para futuros drills, ver `feedback_drills_ux.md` en memoria): las frecuencias del villano van bajo header "El villano, en esta carta" con tinte rosado (son tendencias del rival, no datos sueltos); "Resto"→"Check-call"; "Tu apuesta" muestra el **% del bote**; bote etiquetado "Bote (al apostar)"; **recap narrativo de la acción** ("Flotaste el flop: pagaste $75 con aire. En el turn, si checkea apostás…; si barrelea, foldeás") en vez de jerga; etiquetas de carta legibles (overcard/brick/carta media) · sin tocar `floatEv`/rutas/sitemap/canonical

43. ✅ Drill "¿Qué calculadora?" — **cobertura completa de las 17 calculadoras** + fixes de coherencia · **problema**: el pool derivaba de manos reales vía `suggestCalcForDecision`, que por diseño solo devuelve **6 de 17** calcs como primario (mapea acción→calc) → el alumno solo veía Doble barrel/EV de bluff/Check vs Bet/EV de checkear/All-in EV/Call vs Raise; las otras 11 (EV básico, EV con fold equity, Value/Bluff, EV multi-calle, Implied Odds, EV de flotar, EV del raise, Raise sizing, FE requerida, Fold equity combinada, Call multi-way) **nunca aparecían** · **solución**: `quizSpots.ts` reescrito como **banco escrito a mano** `BANK: Record<CalcMode, Scenario[]>` con **3 escenarios por calc = 51 spots**, cada uno con `context` (eyebrow), cartas/board ilustrativos (helper `cards('Ah Kh')`→`Card[]`), `chips` de bote/sizing, `prompt` en prosa, `correct`, `alternatives` (excluidas de distractores) y `rationale` que enseña el rasgo distintivo de la herramienta · `generateQuizQuestion` elige spot uniforme y arma 4 opciones con distractores que **prefieren el mismo grupo de `CALC_GROUPS`** (los más confundibles) · `WhichCalcDrill.tsx` SpotCard pasa a **data-driven** (lee `spot.context/hero/board/chips/prompt`, ya no `describeSpot`/manos parseadas; chips condicionales) · **`/analisis` intacto** (sigue usando `suggestCalcForDecision`) · **fixes de coherencia de la sesión (en `spotCalc.ts`, benefician también a `/analisis`)**: helper exportado `villainCheckedBeforeHero(hand, decision)` (single source of truth de posición) → un **check de river position-aware**: IP (el rival checkeó antes) → `check-vs-bet` ("check behind vs valuebet"); **OOP** (hero habla primero) → `check-ev` ("EV de checkear": el rival apuesta de farol y pagás vs checkea atrás) — antes mapeaba *cualquier* check de river a `check-vs-bet`, incoherente OOP porque check-behind no existe sin posición y apostar valor fino OOP solo hace foldear faroles · **auto-avance OFF por default** en los 7 drills de Conceptos (`useState(false)`; el usuario lo activa con el toggle si quiere) · **verificado con node**: 51 spots, 17/17 calcs cubiertas (3 c/u), 60k invariantes (4 opciones únicas, correcta presente, alternativas válidas nunca como opción, las 17 salen como correcta) · sin tocar rutas/sitemap/canonical/deploy (sub-drill interno)

44. ✅ Módulo "Ejercicios" — 8º drill de Conceptos: **"Auto-profit raise"** · port del capítulo *"Auto-Profit Raises"* de *The Postflop Poker Workbook* (págs. 166-171) · objetivo: reconocer cuándo un **raise** (de farol/semibluff) ya es rentable **por su sola fold equity**, sin contar la equity cuando te pagan · proceso del libro: (1) calcular el **BE%** del raise, (2) estimar el fold% del rival (Flopzilla), (3) **auto-profit ⇔ fold% > BE%** · **reúso puro de `raiseBluffEv` de `ev.ts`** (fase 33): el BE% del raise = **R/(R+P)** (R = raise total, P = pote en el que subís, ya incluye la apuesta del rival) = `raiseBluffEv().breakevenFoldPct` (`raiseCost/(raiseCost+pot+villainBet)` con pot=pote inicial, villainBet=apuesta, raiseCost=R) — **cero matemática nueva** · el fold% del rival sale de Flopzilla → **se da como dato** (como la equity en SPR y las frecuencias en Floating; no se reconstruye motor de rango/board) · `src/modules/workbook/autoProfitSpots.ts` generador: **plantillas de tamaño curadas** (`TEMPLATES`, números limpios estilo libro — las **5 primeras son las 5 páginas del libro** para que el node matchee exacto) + `breakevenPct` vía `raiseBluffEv` · `AutoProfitKind = be-value | decision` (mix como SPR/Floating): **be-value** MC de 4 con **trampas conceptuales** (complemento 100−BE = P/(R+P), R/P olvidando sumar el raise al denominador, B/(B+P) usando la apuesta del rival en vez del raise, + vecinos); **decision** Sí/No con fold% dado balanceado ~50/50 (target+retry, a veces ajustado tipo 44 vs 47 del libro) + `continueLabel` cosmético bucketeado por magnitud de fold (rango que continúa = origen del fold% dado) · `AutoProfitDrill.tsx` espejo de `FloatDrill` (DecisionButtons Sí/No · ValueOptions 4% · ScoreBar/AutoAdvance OFF default/CountdownBar/atajos 1-2 o 1-4·N), SpotCard con recap de la acción + chips Bote/Apuesta del villano/Tu raise + bloque rosado "El villano, a tu raise" (continueLabel + fold%), FeedbackPanel con BE% sustituido `R/(R+P)` + comparación fold% vs BE% + takeaway (rango que continúa más ancho → menos fold → deja de auto-rentar) · **CalcReveal** → `RaiseBluffEvCalc` (le agregué props `initial*` aditivas) sembrada con fold = fold% dado (decision) o = BE% (be-value → EV≈$0) · soporta `$` (cash) y `K` (torneo) como el libro · 8º tab del grupo Conceptos (icono `BadgePercent`) · **verificado con node**: reproduce EXACTO los 5 BE% del libro (49/62/47/54/49) y los 15 sí/no (`fold% > BE%`) + invariantes en 40k (be-value 4 únicas con BE presente, decision recomputación coincide, balance 50.3%, BE ∈ (0,100)) · HomePage ModuleCard + FAQ a 8 conceptos (JSX + `aPlain` sync) · sin tocar `raiseBluffEv`/rutas/sitemap/canonical/deploy

## Convenciones

- Conventional Commits: `feat(phase-Xa):`, `fix(phase-Xa):`, `docs:`, `chore:`
- Commit al terminar cada sub-fase
- Typecheck + build verdes antes de commitear
- Probar en navegador antes de aprobar fase

## Dominio

Grid 13×13 row-major. Orden: A K Q J T 9 8 7 6 5 4 3 2. Diagonal = pares. Arriba = suited. Abajo = offsuit. Cells con múltiples acciones ponderadas (weight 0-100, suma ≤100).

Acciones: cada `Range` define su propia paleta (`range.actions: ActionDef[]` con `id` opaco, `label`, `color` hex, `order`). Los IDs legacy `RAISE · 3BET · ALL_IN · CALL · FOLD` siguen siendo válidos y se siembran como defaults al hidratar rangos viejos (ver `DEFAULT_ACTION_DEFS` en `src/utils/actionMeta.ts`). Rangos nuevos arrancan con `NEW_RANGE_ACTION_DEFS` (Call + Raise).

## Arquitectura clave

- `src/components/RangeGrid/` — grid reutilizable, modo lectura y `editable` opt-in (props: `editable`, `onCellPaint`, `onCellErase`). Delega eventos por `data-hand` sin tocar la memoización de RangeCell.
- `src/store/rangeStore.ts` — CRUD + persist con Zod al hidratar, cap 100KB, IDs con `crypto.randomUUID()`.
- `src/store/selectors.ts` — `useActiveRange`, `useRangeSummaries` (useShallow), `useRangesByGroup` (useMemo).
- `src/modules/{home,viewer,editor,trainer}/` — una carpeta por módulo, página raíz + componentes locales. `/` monta `home/HomePage.tsx` con onboarding + `ImportProfileButton` (consume `importRanges` del store).
- `src/utils/handUtils.ts` — `ALL_HANDS`, `categoryOf`, `combosOf`.
- `src/utils/rangeStats.ts` — `computeRangeStats` (%total, combos, presentActions).

## Reglas importantes

- TypeScript estricto, sin `any`
- `React.memo` en RangeCell NO se toca — todo cambio pasa por props estables
- Persistencia en localStorage keys `range-soprano/ranges` y `range-soprano/ui`
- IDs vía `crypto.randomUUID()`
- Parser de import tolerante a errores de formato
- NO implementar GTO ni MDA

## Notas para Claude Code

- Antes de modificar código: `git status`, revisar commits recientes
- Si el usuario reporta bug: diagnosticar causa raíz antes de proponer fix
- Presentar plan, esperar confirmación, luego codificar
- Al terminar: `npm run typecheck && npm run build`, luego commit